
module sample.mod_1;

package sample.one( plugin_package )
  : "This is a package that takes another package as a parameter."
    details( """
      The plugin package would need to implement a given package interface. Need
      to think about what that means ...
    """ )
begin

  structure type Example : defined_in( "one/Example.barlom" );

  function doIt : defined_in( "one/doIt.barlom" );

  object type MyWidget : defined_in( "one/MyWidget.barlom" );

  package innards : defined_in( "one/innards.barlom" );

end







package sample.two( plugin-package );